# O Netlify publicava a raiz deste repositorio como site estatico. A fase 1 acabou
# com isso: as telas foram para `apps/web/src/`, o build sai em `apps/web/dist/` e
# quem serve e o Hono, atras da sessao do better-auth.
#
# A marca `!` no fim e o que faz esta regra valer. Sem ela o Netlify serve o
# arquivo que existe e ignora o redirecionamento, entao `docs/apolice-*.pdf` e os
# discos de tacografo dos 15 veiculos continuariam baixaveis por quem soubesse o
# caminho. Com ela, todo caminho responde a pagina de aviso.
#
# A fase 4 troca isto pelo endereco definitivo.
/*  /indisponivel.html  200!
